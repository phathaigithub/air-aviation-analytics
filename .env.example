# Copy this file to .env and adjust the local SQL Server connection.
# Never commit the real connection string.
SQLSERVER_CONNECTION_STRING=DRIVER={ODBC Driver 18 for SQL Server};SERVER=localhost;DATABASE=AirReviews;Trusted_Connection=yes;Encrypt=yes;TrustServerCertificate=yes
